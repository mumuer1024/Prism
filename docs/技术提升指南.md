# 团队技术提升指南

> 基于 Prism 项目现状分析，为团队提供系统性的技术改进建议

---

## 📋 当前项目评估

| 维度 | 评分 | 说明 |
|:--|:--|:--|
| 类型标注 | ⭐⭐ | 大量函数缺少类型提示 |
| 文档注释 | ⭐⭐⭐ | 核心模块有文档 |
| 错误处理 | ⭐⭐⭐ | 基础已实现 |
| 测试覆盖 | ⭐⭐ | API 测试完善，传感器模块缺乏 |
| 性能优化 | ⭐⭐ | 串行采集，可并行优化 |
| 架构解耦 | ⭐⭐⭐ | 模块化较好 |

---

## 🎯 提升计划

### 阶段一：代码质量基础（1-2周）

#### 1.1 类型标注 (Type Hints)

**改进示例**:

```python
# ❌ 改进前
def fetch_top_stories(limit):
    return []

# ✅ 改进后
from typing import List, Optional

def fetch_top_stories(limit: int = 10) -> List[HNStory]:
    """Fetch top stories from Hacker News."""
    ...
```

**任务**:
- [ ] `src/sensors/` 所有函数添加类型标注
- [ ] `src/database/crud.py` 类型标注
- [ ] `llm_client.py` 类型定义

#### 1.2 错误处理标准化

```python
# src/utils/errors.py

import logging
from functools import wraps
from typing import Callable, TypeVar

logger = logging.getLogger(__name__)
T = TypeVar('T')

def handle_errors(func: Callable) -> Callable:
    """统一错误处理装饰器"""
    @wraps(func)
    def wrapper(*args, **kwargs):
        try:
            return func(*args, **kwargs)
        except Exception as e:
            logger.exception(f"{func.__name__} error: {e}")
            raise
    return wrapper

# 使用
@handle_errors
def fetch_data(url: str) -> dict:
    ...
```

**任务**:
- [ ] 创建 `src/utils/errors.py`
- [ ] 关键函数添加错误处理

---

### 阶段二：测试完善（2-3周）

#### 当前测试覆盖

| 模块 | 状态 | 优先级 |
|:--|:--|:--|
| `src/auth/` | ⭐⭐⭐⭐ 完善 | 保持 |
| `src/sensors/` | ⭐ 缺失 | **高** |
| `llm_client.py` | ⭐ 缺失 | 高 |

#### 传感器测试示例

```python
# tests/test_sensors/test_hacker_news.py

import pytest
from unittest.mock import Mock, patch
import httpx
from src.sensors.hacker_news import HNStory, fetch_top_stories

class TestHNStory:
    def test_hn_url_property(self):
        story = HNStory(id=12345, title="Test", url=None, score=100, by="user", descendants=50)
        assert story.hn_url == "https://news.ycombinator.com/item?id=12345"

class TestFetchTopStories:
    @patch('src.sensors.hacker_news.httpx.get')
    def test_fetch_success(self, mock_get):
        mock_response = Mock()
        mock_response.json.return_value = [123]
        mock_item = Mock()
        mock_item.json.return_value = {"id": 123, "type": "story", "title": "Test", "url": "https://x.com", "score": 100, "by": "user", "descendants": 50}
        mock_get.side_effect = [mock_response, mock_item]
        
        stories = fetch_top_stories(limit=1)
        assert len(stories) == 1
        assert stories[0].title == "Test"
```

**任务**:
- [ ] 创建 `tests/test_sensors/` 目录
- [ ] 为每个传感器编写单元测试
- [ ] 添加 LLM 客户端 mock 测试

---

### 阶段三：架构优化（2-3周）

#### 3.1 配置管理统一 (Pydantic)

```python
# src/config.py

from pydantic import BaseModel, Field
from pydantic_settings import BaseSettings

class DatabaseConfig(BaseModel):
    url: str = "sqlite:///data/prism.db"
    echo: bool = False

class Settings(BaseSettings):
    database: DatabaseConfig = Field(default_factory=DatabaseConfig)
    
    class Config:
        env_file = ".env"

settings = Settings()
```

#### 3.2 数据采集并行化

```python
# src/utils/parallel.py

from concurrent.futures import ThreadPoolExecutor, as_completed
from typing import List, Callable, Iterable, TypeVar

T = TypeVar('T')
R = TypeVar('R')

def parallel_map(func: Callable[[T], R], items: Iterable[T], max_workers: int = 5) -> List[R]:
    results: List[R] = [None] * len(items)
    items_list = list(items)
    
    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        future_to_index = {executor.submit(func, item): i for i, item in enumerate(items_list)}
        for future in as_completed(future_to_index):
            idx = future_to_index[future]
            results[idx] = future.result()
    
    return results
```

---

### 阶段四：性能优化（持续）

#### 4.1 缓存机制

```python
# src/utils/cache.py

import time
from functools import wraps
import hashlib
import json

def cached(ttl_seconds: int = 300):
    cache = {}
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            key = hashlib.md5(f"{func.__name__}:{args}:{kwargs}".encode()).hexdigest()
            if key in cache:
                result, timestamp = cache[key]
                if time.time() - timestamp < ttl_seconds:
                    return result
            result = func(*args, **kwargs)
            cache[key] = (result, time.time())
            return result
        return wrapper
    return decorator

@cached(ttl_seconds=600)
def fetch_github_trending(language: str = "python") -> List[dict]:
    # 实际 API 调用
    ...
```

---

### 阶段五：代码规范

#### 5.1 Git 提交规范

```
feat: 新功能
fix: 错误修复
docs: 文档更新
style: 代码格式（不影响功能）
refactor: 重构
test: 测试相关
chore: 构建/工具链
```

#### 5.2 代码审查清单

- [ ] 类型标注完整
- [ ] 文档字符串齐全
- [ ] 错误处理恰当
- [ ] 测试覆盖核心逻辑
- [ ] 无硬编码配置
- [ ] 日志记录完整

---

## 📈 实施建议

| 阶段 | 时间 | 重点 |
|:--|:--|:--|
| 第一阶段 | 1-2周 | 类型标注 + 错误处理 |
| 第二阶段 | 2-3周 | 完善测试覆盖 |
| 第三阶段 | 2-3周 | 架构优化 |
| 第四阶段 | 持续 | 性能优化 |

建议采用 **敏捷迭代**，每两周一个阶段，每个阶段有明确的产出物（如测试文件、文档、代码重构）。